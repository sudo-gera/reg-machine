# from dataclasses import dataclass, field
# from functools import cached_property

# @dataclass(frozen=True)
# class FA:
#     states : tuple[str] = field(default_factory=tuple)
#     letters : tuple[str] = field(default_factory=tuple)
